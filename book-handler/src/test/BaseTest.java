import com.fmyl.books.handler.utils.BaseUtil;
import com.fmyl.books.handler.utils.LoggerUtils;

/**
 * Created by fuzq on 17/9/7.
 */
public abstract class BaseTest {
    protected static LoggerUtils logger = LoggerUtils.getInstance(BaseUtil.class);
}
